{
  "name": "linksprout",
  "homepage": "https://linksprout.github.io/",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build", // This runs the build and creates the 'dist' folder
    "preview": "vite preview",
    "predeploy": "npm run build", // This correctly calls the existing 'build' command
    "deploy": "gh-pages -d dist" // This now points to the 'dist' folder
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "gh-pages": "^6.3.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
